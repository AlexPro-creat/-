# Задачник для торговых агентов (дистрибуция косметики)

Веб-приложение для управления визитами торговых агентов к контрагентам (салоны красоты, мед.клиники и т.п.): карточка клиента с данными по маршруту, регулярный ассортимент по продажам, задолженность, доска задач-визитов и вложения (фото/аудио). Работает на телефоне и компьютере через браузер.

Написано на «чистом» Node.js — **без единой внешней библиотеки** (нет ни Express, ни React, ни базы данных вроде Postgres). Это осознанное решение: приложение запускается командой `node src/server.js` без `npm install`, что сильно упрощает установку и последующую поддержку. Все данные хранятся в одном файле `data/db.json` и в папке `data/uploads/` (вложения к задачам).

## Возможности

- **Карточка контрагента**: тип точки, адрес, телефон, контактное лицо, день визита, статус «работает по договору», способ оплаты (нал/безнал/QR), ответственный агент. Эти поля редактирует **только администратор** — торговый агент видит их, но не может менять (кроме заметок).
- **Таблица клиентов**: колонки Название (закреплена слева при горизонтальной прокрутке) → Долг → Ассортимент риск → Открыть → Тип точки → Номер телефона → Контактное лицо; клик по заголовку «Долг» или «Ассортимент риск» сортирует список. Есть быстрый поиск по названию/телефону/контактному лицу.
- **Фильтры по клиентам**: день недели визита, тип точки, только постоянные клиенты (есть регулярный ассортимент), только с задолженностью, «не добрал» (есть товары с риском «отвала» в регулярном ассортименте).
- **Регулярный ассортимент**: список товаров, которые клиент стабильно заказывает (рассчитан по продажам за последние 7 месяцев), отдельным блоком — товары с риском «отвала» (не заказывали в последнем доступном месяце).
- **Тестовый ассортимент**: отдельный список товаров, которые клиент покупал хотя бы раз, но не так регулярно, чтобы попасть в основной ассортимент (разовые/пробные покупки) — виден в карточке клиента и прямо в карточке задачи.
- **Задолженность**: сумма долга и отметка о просрочке — подтягивается из выгрузки по должникам.
- **Доска задач** (воронка визитов): В работе → Выполнена → Не выполнена. Задача сразу создаётся «В работе» (без промежуточного «Новая задача»). У каждой задачи можно проставить теги (Kapous, EPICA, Чистовье, Палитра, новый клиент, Лист ожидания, Прогрев) и отфильтровать доску по ним — «Лист ожидания» и «Прогрев» теперь теги, а не отдельные этапы. Перетаскивание карточек мышью.
- **Срок задачи**: обязателен — без даты задачу не сохранить. Агент задаёт её один раз (при создании); после этого менять срок может только администратор или супервайзер.
- **Отчёт по задаче**: отдельное поле — без заполненного отчёта нельзя перевести задачу в «Выполнена». На карточке задачи заполненный отчёт отмечен зелёной галочкой.
- **Ассортимент в задаче**: из карточки задачи можно раскрыть регулярный (и тестовый) ассортимент клиента, не открывая отдельно его карточку.
- **Календарь**: отдельная вкладка с видами месяц / неделя / день. В мобильной версии на клеточке даты показывается цветная точка с числом незавершённых задач — зелёная, если срок ещё не наступил, красная — если просрочено.
- **История визитов**: по каждому клиенту видно все прошлые задачи с комментариями агента.
- **Вложения к задаче**: фото (скриншоты заказов, накладных) и аудиозаписи прямо в карточке визита.
- **Аватары сотрудников**: администратор может загрузить фото для каждого агента (раздел «Команда») — отображается рядом с именем в списках и на канбане для быстрого узнавания.
- **Роли**:
  - **Администратор** — полный доступ, включая управление сотрудниками и резервное копирование.
  - **Супервайзер** — видит и координирует всех агентов и клиентов; права почти полностью дублируют администратора (редактирование защищённых полей клиента, подтверждение точек, массовая перепривязка, удаление, экспорт CSV, печать маршрута), кроме управления сотрудниками и резервного копирования.
  - **Торговый агент** — видит и работает только со своими клиентами и задачами.
  - **Удаление** клиентов и задач доступно только администратору и супервайзеру — торговые агенты удалять не могут.
- **Новые точки вне маршрута**: если агент добавляет клиента, которого не было в исходной выгрузке, точка помечается «вне маршрута» и «на согласовании» — администратор увидит её на дашборде и подтверждает.
- **Дубли при добавлении клиента**: при создании нового контрагента система проверяет совпадение по названию, адресу и телефону одновременно — если всё совпадает, предупреждает о возможном дубле.
- **Дашборд**: количество клиентов, задачи на сегодня/просроченные, клиенты с риском «отвала» ассортимента, общий долг, новые точки на согласовании; список клиентов на сегодня, список клиентов на сегодня с задолженностью, список просроченных задач, выполнение по каждому агенту (% выполненных визитов), **общее количество задач по всей команде** (карточка «Всего задач по команде» + строка «Итого по команде» в таблице по агентам — видно супервайзеру и администратору), долг в разбивке по каждому агенту, клиенты без задачи на текущую неделю, число заявок на подтверждение (новые точки + закрытие точек).
- **Мой день** (только у торгового агента): упрощённый вид на сегодняшние задачи крупными карточками с кнопками «Позвонить» и «Маршрут» (открывает точку в Google Картах).
- **Клик по номеру телефона** в карточке клиента, таблице клиентов и «Моём дне» сразу открывает звонок на телефоне.
- **Фильтр по способу оплаты** (наличка/перечисление) в таблице клиентов, а также фильтр «показывать закрытые точки».
- **Дата/время создания задачи и автор** — видно администратору и супервайзеру в карточке задачи.
- **Просрочка по дням** — на бейдже задолженности показывается количество дней с даты поставки (из выгрузки по долгам), если дату удалось разобрать.
- **Бейджи в навигации**: на вкладке «Задачи» — число просроченных задач, на вкладке «Клиенты» — число точек, ожидающих подтверждения.
- **Автосоздание следующей задачи**: при первом переводе задачи в «Выполнена», если у клиента указан день визита и нет другой активной задачи в будущем, система сама создаёт следующую задачу на ближайшую подходящую дату.
- **Архив задач**: выполненную задачу должен подтвердить администратор или супервайзер, переведя её в «Архив» — только тогда она пропадает из «Выполнена» у агента (у агента этап «Архив» не отображается вовсе). Массовое удаление задач (с чекбоксами) — доступно только администратору и супервайзеру.
- **Несколько вложений за раз**: к задаче можно прикрепить сразу несколько фото/аудио одним выбором файлов.
- **Заметки по звонкам/контактам** — отдельный от задач лёгкий журнал заметок в карточке клиента (кто и когда оставил).
- **«Точка закрыта»**: агент может отправить запрос на закрытие точки; администратор/супервайзер подтверждает (точка помечается закрытой и её можно скрыть фильтром) или отклоняет, точку всегда можно открыть заново.
- **Дата последнего визита** — видна прямо в карточке клиента; если давно не был у клиента, это дополнительно подсвечено.
- **Экспорт клиентов в CSV** — доступен только супервайзеру.
- **Массовая перепривязка клиентов** к другому агенту (с чекбоксами) — доступна администратору и супервайзеру.
- **Печать маршрута на неделю** (только супервайзер) — кнопка на вкладке «Календарь» формирует список визитов по агентам на текущую неделю и открывает системное окно печати браузера (можно сохранить как PDF средствами браузера).
- **Уведомления о задачах**: при первом входе браузер сразу запрашивает разрешение на уведомления; пока вкладка приложения открыта (в том числе свёрнута/в фоне), система каждые несколько минут проверяет задачи на сегодня/просроченные и присылает уведомление. Это не серверные push-уведомления — при полностью закрытой вкладке/браузере уведомления приходить не будут (см. «Известные ограничения»).
- **Резервная копия одним нажатием** — кнопка «Скачать резервную копию» на вкладке «Команда» (только администратор) скачивает ZIP-архив с базой данных и всеми вложениями.
- **Права супервайзера расширены и во многом дублируют администратора**: супервайзер теперь тоже может редактировать защищённые поля карточки клиента, подтверждать новые точки, подтверждать/отклонять закрытие точек, делать массовую перепривязку клиентов. Управление сотрудниками (создание/удаление пользователей, смена ролей) и резервное копирование остаются только у администратора.

## Новое в Фазе 6

- **Мастера/сотрудники точки**: в карточке клиента — список мастеров (маникюр, парикмахер, стоматолог, массажист и т.п.) с именем, телефоном и специализацией. Агент добавляет мастера сразу (без согласования), новый мастер помечается «новый» до просмотра администратором/супервайзером — счётчик непросмотренных виден на дашборде.
- **6 новых полей в карточке клиента** (редактирует только администратор/супервайзер): ИНН/реквизит, контакт в мессенджере, удобное время звонка, ЛПР (кто принимает решение), особые пожелания, «принимает заявки только в определённое время» (информационное поле, без автоблокировки).
- **Ассортимент теперь с брендом, выручкой и маржой**: в списках регулярного/тестового ассортимента у каждого товара — бренд (Kapous/EPICA/Чистовье/Палитра/Прочее, определяется по названию), сумма продаж и маржа за 7 месяцев. Фильтр по бренду (в т.ч. «Красители/оксиды» одной кнопкой) — удобно готовить предложение по конкретному бренду.
- **Вкладка «Отчёты»** (только супервайзер/администратор): сводная таблица «сколько какого товара продал каждый агент» — товар, бренд, штук, выручка, число клиентов, с тем же фильтром по бренду.
- **Дашборд агента**: «Мои показатели» — продано за 7 мес., клиентов с продажами, звонков/встреч сегодня, выполнено сегодня, просрочено, топ-5 товаров.
- **Дашборд супервайзера дополнен**: задачи на день по команде (выполнено/не выполнено на агента), рейтинг клиентов (выручка/маржа/активные месяцы), счётчик новых мастеров.
- **Воронка продаж «звонок → встреча → сделка/провал»** — отдельная вкладка внутри «Задачи» («Воронка продаж»), независимая от обычной доски визитов. Звонок остаётся открытым до следующего шага, встреча требует аудиозаписи и короткого пояснения — без них задачу нельзя закрыть в «Сделка» или «Провал». Проваленные задачи можно выгрузить в CSV (кнопка/эндпоинт `/api/tasks/failed-export`) и — при настроенном Google-аккаунте — автоматически отправляются строкой в Google Таблицу (см. ниже).
- **Заявка на перенос даты задачи**: агент теперь не «упирается» в заблокированную дату — может отправить заявку с желаемой датой и причиной; администратор/супервайзер одобряет (дата меняется сама) или отклоняет прямо в карточке задачи.
- **Встречи супервайзера с клиентом — в календаре**: у супервайзера в календаре кнопка «+ Встреча с клиентом» (дата/время/заметка); фиолетовая точка в месячном виде и отдельный раздел «👔 Супервайзер» в недельном/дневном виде показывают агентам, какие дни у клиента уже заняты. Удалять встречу может только супервайзер.
- **Обновление остатков/продаж/долгов больше не трогает задачи и другую введённую информацию** — при повторном импорте меняются только расчётные поля (ассортимент, долг), карточка клиента, мастера, заметки и все задачи сохраняются как есть.

### Что нужно от вас, чтобы включить оставшиеся пункты

Часть пунктов из списка требует дополнительных файлов/доступов и пока не подключена:

- **Автовыгрузка «провала» в Google Таблицу** — код готов (`src/googleSheets.js`), но нужен файл ключа сервисного аккаунта Google Cloud и доступ на редактирование к самой таблице; пока не настроено — работает ручной CSV-экспорт без каких-либо действий с вашей стороны.
- **Шаблон загрузки остатков/продаж/долгов/задач через браузер** (без участия Claude между обновлениями) — отложено по вашей явной просьбе, обсудим отдельно, когда решите к этому вернуться.

## Новое после Фазы 6: три новых агента, остатки на позициях, «Акции»

По ссылкам на Google Диск, которые вы прислали, добавлено:

- **Три новых торговых агента с их клиентами**: Батаева (111 точек, регион), Жанара (59 магазинов — у всех тип точки проставлен «Магазин», день визита оставлен пустым, как вы и просили), Анастасия (117 точек, с 08.09.2026 отображается как «Бегимай» — логин/пароль тот же, см. таблицу ниже). Логины — см. таблицу ниже. Клиенты добавлены в общую базу тем же безопасным механизмом, что и раньше: если имя контрагента уже встречалось у одного из прежних трёх агентов, новая карточка не создаётся (чтобы не задвоить), а два случая такого совпадения оставлены как есть — при необходимости скажите, и разберём вручную.
- **Остаток на складе — прямо на позиции ассортимента**: в карточке клиента у каждого товара в регулярном/тестовом ассортименте теперь показан бейдж «📦 N шт/упаковка» — берётся напрямую из вашей выгрузки актуальных остатков по точному совпадению названия товара. Если бейджа нет — значит, для этого товара не нашлось соответствия в остатках (примерно 1 позиция из 9, в основном из-за того, что названия товаров в файлах продаж исторически обрезаны до 70 символов); это не то же самое, что «остаток 0», а именно «нет данных». Никакого распределения остатка между конкурирующими клиентами не делали — по вашей просьбе это просто прямой показ количества.
- **Раздел «Акции» в карточке клиента**: список того, что клиент брал по текущим акциям склада/магазина (название акции + количество), из вашей выгрузки `Загрузка_акции_25.08.xlsx`. Раздел появляется только у тех клиентов, для которых есть данные (24 из 46 строк выгрузки совпали с текущей базой клиентов — остальные, похоже, относятся к точкам продаж компании напрямую, а не к клиентам конкретных торговых агентов).
- **Таблицы из папки «шаблон для загрузки новых данных»** (остатки, акции, продажи с 01 по 25.08, долги на 25.08) взяты как образец будущего формата, как вы и просили — сейчас из них подключены только остатки и акции (см. выше); продажи/долги за неполный август намеренно не подмешивали в общий 7-месячный расчёт, чтобы не исказить помесячную статистику — дождёмся полного месяца.
- Шаблон массовой загрузки через браузер — отложен, как вы и попросили.

Технически: три новых файла в `data/import/` — `stock.json` (остатки, ключи — нормализованное название товара), `promotions.json` (акции, ключи — название клиента), плюс новая идемпотентная `ensureExtraAgents()` в `src/import.js` (в отличие от `ensureTeam()`, которая создаёт всю команду только на пустой базе, эта добавляет только недостающих из списка агентов на каждом старте — важно, потому что база уже не пустая).

## Новое после Фазы 6.1: дашборд агента за месяц, «Лист ожидания», права на контакты, отчёты с фильтрами

Большой раунд точечных правок по итогам работы с интерфейсом:

- **Дашборд агента — теперь про текущий месяц, а не 7 месяцев**: карточка «Продано» показывает сумму продаж за текущий месяц по всем клиентам агента (раньше — 7-месячный итог), карточка «Клиентов с продажами» — формат «купили/не купили» (например, «49/68») тоже за текущий месяц.
- **Топ-10 по трём брендам за месяц** — свёрнуто по умолчанию (кнопка «🏆 Топ-10 по брендам»), отдельная таблица на Kapous/EPICA/Чистовье; в Kapous и EPICA красители и оксиды в топ не попадают (в Чистовье — попадают, там это уместно).
- **Карточка «Клиентов с риском отвала товара» выделена визуально** (красная рамка/подложка) — она у же была на дашборде, теперь её сложнее не заметить.
- **Новая логика «постоянный клиент»**: раньше — если хотя бы один товар покупался в 4+ из последних 7 месяцев (регулярный ассортимент); теперь — если у клиента была **любая** покупка в каждый из **последних 3 месяцев подряд**, независимо от товара. 7-месячное окно регулярного/тестового ассортимента не менялось — это отдельный, узкий показатель именно для флага «постоянный клиент».
- **Ежемесячный архив продаж** — по-прежнему через отдельную сессию с Claude (загрузка нового месяца, прошедший месяц уходит в архив, вся динамическая история — ассортимент, «постоянный клиент» и т.п. — пересчитывается); от отдельной кнопки загрузки в самом приложении сознательно отказались — безопасный разбор Excel в браузере потребовал бы либо внешней библиотеки (нарушает принцип «ноль зависимостей»), либо серьёзной ручной работы с риском ошибок в подсчётах.
- **Год в полях даты — двумя цифрами** (ДД.ММ.ГГ) во всех местах, где раньше был `дд.мм.гггг`: срок задачи, перенос даты, встреча супервайзера — с календариком по кнопке 📅 и ручным вводом; значения хранятся и передаются на сервер как раньше, поменялось только отображение.
- **Воронка «Визиты» переименована в «Визиты с супервайзером»** — пока только смена названия вкладки, отдельную карточку/логику под эту тему сделаем по вашей отдельной спецификации.
- **Новая воронка «Лист ожидания»** — для товара под заказ, с этапами «Клиент ждёт товар» → «Накладная оформлена» → «Товар получен клиентом» (закрытая — подтвердить может только администратор/супервайзер, по аналогии с «Архивом» у визитов). Теги — Kapous/EPICA/Чистовье/Палитра (какой бренд ждут).
- **Телефоны нормализованы**: везде, где номер не начинался с «0», ведущий ноль добавлен, лишние пробелы внутри номера убраны — это же теперь происходит автоматически при любом редактировании телефона (импорт, карточка клиента).
- **Иконка WhatsApp рядом с номером телефона** — всюду, где раньше была только иконка звонка; открывает переписку в WhatsApp (номер переводится в международный формат автоматически).
- **Карта в карточке клиента теперь ведёт в 2ГИС** (`2gis.kg/bishkek`) вместо Google Карт.
- **Краски и оксиды — отдельная видимая группа** внутри регулярного и тестового ассортимента (а не только фильтр по кнопке) — раздел «риск отвала» остаётся единым списком, без такого разделения, как и раньше.
- **Права агента на редактирование адреса/телефона/контактного лица** — новая галочка у каждого агента в разделе «Команда» (по умолчанию выключена); если включить, агент сможет менять эти поля у своих клиентов, как администратор/супервайзер.
- **Телефон клиента — в карточке задачи** во всех трёх воронках (визиты, продажи, лист ожидания).
- **Календарь супервайзера (день)**: у каждой встречи теперь видно и клиента, и ответственного агента — удобно смотреть день и сразу понимать, чей это клиент.
- **Дашборд супервайзера/администратора**: на каждой из пяти верхних карточек (клиенты, задачи на сегодня, просроченные, риск отвала, общий долг) — свой выпадающий фильтр по агенту («Все агенты» по умолчанию); карточка долга дополнительно фильтруется по форме оплаты.
- **Кнопка «Отмена» в карточке клиента** — приглушённый красный цвет вместо яркого.
- **Вкладка «Отчёты»**: в «Ассортимент по агентам» добавлены фильтр по агенту (с опцией «Все агенты» — сумма по всей команде) и фильтр по месяцу («Все месяцы» — прежняя 7-месячная агрегация, либо конкретный месяц — тогда показаны данные только за него); контраст кнопок фильтра по бренду исправлен (буквы белые, раньше сливались с фоном). Добавлен отдельный отчёт **«🎁 Акции»** — кто из клиентов что брал по акциям, с разбивкой по агенту.
- **Метка и фильтр «Есть акции»** — на странице «Клиенты» у карточки клиента с активными акциями теперь бейдж «🎁 акции», плюс отдельный фильтр-чекбокс в списке.
- **Бренд по справочнику номенклатуры**: проанализирован присланный файл «Справочник номенклатура.xls» — отдельного столбца с брендом там нет, но найдена надёжная закономерность: товары, начинающиеся на «E » (например «E Шампунь…»), почти всегда относятся к EPICA (639 из 640 таких позиций в справочнике) — это правило добавлено в определение бренда и переклассифицировало часть товаров из «Прочее» в EPICA. Для Kapous/Чистовье такой закономерности в названиях не нашлось — там всё ещё определение по ключевым словам. **Палитра не тронута** — её определение осталось прежним, как вы и попросили.
- **Ускорен первый запуск на новой базе**: при самом первом старте (или после сброса `data/db.json`) заполнение полей по умолчанию для сотен клиентов теперь идёт одной пакетной записью на диск вместо отдельной на каждого клиента — раньше это могло занимать десятки секунд, теперь — доли секунды.

## Новое 02.09.2026 (Фаза 17): комбинируемые фильтры красок/оксидов, загрузка клиентов, кнопка «Наверх»

- **Отчёт «Ассортимент по агентам» — фильтр по бренду стал мультивыбором** (можно отметить сразу несколько чипов, например Kapous + EPICA + Studio), а «Красители/оксиды» вынесен в отдельный трёхпозиционный переключатель («Все категории» / «Только краски/оксиды» / «Убрать краски/оксиды»), который сочетается с выбором бренда — например, Kapous + «Только краски/оксиды» покажет краски/оксиды именно у Kapous, а не у всех брендов сразу.
- **Фильтр «Убрать краски/оксиды»** — тот же переключатель добавлен и в карточку клиента (регулярный и тестовый ассортимент, отдельно друг от друга) рядом с чипами бренда — позволяет скрыть эту группу из общего списка ассортимента.
- **Кнопка «⬆ Наверх» на дашборде** — плавающая кнопка внизу справа, видна всем ролям, появляется после прокрутки страницы вниз именно на вкладке «Дашборд». *(Убрана полностью в Фазе 18 — пользователь передумал, см. ниже.)*
- **Кнопка «⬆ Загрузить клиентов»** на странице «Команда» (только администратор) — пара к уже существующей «⬇ Выгрузить клиентов»: загружает ранее выгруженный JSON-файл и обновляет ручные поля уже существующих клиентов, найденных по имени и агенту; новых клиентов не создаёт, дублей не бывает — можно безопасно загрузить один и тот же файл повторно.

## Новое 02.09.2026 (Фаза 18): новое правило регулярного ассортимента, «недопродано» = фокус на допродажу

- **Правило регулярного ассортимента изменено полностью** (было: минимум в 3 из последних 5 месяцев + разрыв не больше 2 подряд — старое правило сохранено ниже как исторический контекст в статус-документе). Новое правило: товар куплен **в обоих последних 2 месяцах** окна (это подтверждает, что клиент берёт его прямо сейчас) **И** минимум в **4 из последних 6 месяцев** окна (подтверждает, что покупка не случайная). Окно расширено с 5 до 6 месяцев — сейчас это март–август, динамически (сдвигается, когда в данных появляется новый закрытый месяц, не завязано на системную дату). Пересчитано на реальных данных: 502 позиции у 161–164 клиентов (было 1309/303 по старому правилу — новое правило заметно строже, целенаправленно оставляет только то, что клиент покупает прямо сейчас на постоянной основе).
- **«Недопродано» переопределено**: теперь это «постоянный ассортимент минус то, что уже куплено в текущем месяце» — так же прямо, как попросили, чтобы дать агенту фокус на допродажу регулярного ассортимента, а не просто исторический флаг риска. Технически — раньше это был флаг, который прибивался гвоздями один раз при пересборке ассортимента (сравнение с последним месяцем самого окна классификации); теперь считается заново при каждой отдаче клиента, сверяя постоянный ассортимент с отдельным потоком «продажи текущего месяца» (`currentMonthItems`, тот же источник, что уже питал карточки дашборда) — поэтому обновляется сразу, как только вы загрузите свежий срез продаж текущего месяца, не дожидаясь полной пересборки всего ассортимента. Пока новый месяц не прислан (тот же механизм «протухания», что и раньше) — весь постоянный ассортимент честно показывается как «недопродано», это и есть корректное поведение: в начале месяца ещё ничего не продано.
- Метка в карточке клиента обновлена: «X из 5 посл. мес» → «X из 6 посл. мес».
- **Кнопка «⬆ Наверх» убрана полностью** — из `index.html`, `app.js` и `styles.css`, без остатка (была добавлена в Фазе 17, пользователь передумал сразу после сдачи).
- **Поиск клиента при создании задачи**: в модалке «Новая задача» над полем «Клиент» появилось текстовое поле поиска — сужает список клиентов в выпадающем списке по подстроке в названии (без учёта регистра), по аналогии с поиском на странице «Клиенты». Сам `<select>` остаётся источником истины при отправке формы, так что случайно отправить нерабочий/пустой выбор нельзя. Поле поиска есть только при создании новой задачи — при редактировании клиент задачи всё так же заблокирован (менять его нельзя).

## Новое 04.09.2026 (Фаза 19): «тип точки» — выпадающий список, загрузка долгов/остатков без пересборки

- **«Тип точки» стал выпадающим списком** из 8 категорий (Салон красоты, Медицинский центр/клиника, Стоматология, Магазин, Спа-салон, Салон депиляции, Массажный салон, Другое) — раньше было свободное текстовое поле, из-за чего накопилось 31 вариант написания одного и того же (регистр, «Магазин»/«Магазины» и т.п.). При первом запуске на уже существующей базе все прежние значения автоматически приводятся к этому списку (безопасно, срабатывает один раз на каждого клиента — повторные запуски ничего не меняют). Редкие варианты (лаборатория, барбершоп, детсад, аптека, оптовый клиент) ушли в «Другое».
- **Загрузка долгов и остатков прямо через панель «Команда»** — две новые кнопки «⬆ Загрузить долги» и «⬆ Загрузить остатки» (только администратор), рядом с «Загрузить клиентов». Файл — **.xlsx или .csv** (старый бинарный `.xls` этим путём не читается — в приложении принципиально нет внешних библиотек и LibreOffice на сервере, поэтому нужен файл в новом формате или CSV; при выгрузке из 1С/склада это обычно одна галочка «сохранить как»). Колонки ищутся по синонимам заголовка (например, «Контрагент»/«Клиент»/«Наименование» для имени, «Долг»/«Задолженность»/«Сумма» для суммы) — если заголовки совсем не похожи на ожидаемые, эндпойнт вернёт понятную ошибку с найденными заголовками вместо того, чтобы угадывать. Оба эндпойнта работают как **полная замена снимка** — точно так же, как обычный импорт при пересборке: клиент, которого нет в новом файле, теряет долг/остаток (уходит в 0/«нет данных») — это осознанное поведение «срез на дату», не баг. Долг по-прежнему сопоставляется с клиентом только по имени (без учёта агента, по решению пользователя — то же ограничение, что и раньше). Дата «снято на» для остатков теперь обновляется автоматически датой сервера при каждой загрузке (раньше была захардкожена).
- Оба файла (`data/import/debts.json`, `data/import/stock.json`) после загрузки через панель сохраняются на диск — данные переживут перезапуск/передеплой сервера, не только текущую сессию.

## Новое 04.09.2026 (Фаза 20): шапка с кнопкой «Выйти», название «CRM Cosmedeca», артикул товара, свайп для закрытия карточки, «Последние продажи»

- **Кнопка «Выйти» всегда рядом с названием приложения** — шапка теперь двухрядная: сверху название + кнопка «Выйти» (не расходятся на узких экранах), снизу отдельной строкой — вкладки навигации, которые переносятся сами по себе, не утягивая кнопку «Выйти» за собой.
- **Название приложения — «CRM Cosmedeca»** (было «Задачник · Косметика») — в шапке, в заголовке вкладки браузера и на экране входа.
- **Артикул товара** — теперь виден рядом с названием во всех трёх списках: регулярный ассортимент, тестовый ассортимент и «Последние продажи» (см. ниже). Источник — 5 присланных прайс-листов поставщика (Чистовье, EPICA, Kapous/Studio/HY, Перчатки, Прочие ТД КМ), сопоставлены по точному совпадению нормализованного названия товара (`data/import/articles.json`, 2182 позиции). Покрытие по факту ~92% товарных строк в ассортименте клиентов — не находят пару в основном позиции декоративной косметики (бренд «AV/ES/PRO/MS», прайса на неё не присылали) и товары с сильно обрезанными/нетиповыми названиями; для них бейдж артикула просто не показывается, а не пишется «нет данных».
- **Закрытие карточки клиента свайпом вправо** — работает на сенсорных экранах (телефон/планшет), в дополнение к обычным кнопкам «Отмена»/клику вне карточки. Свайп, начавшийся на чипах-фильтрах бренда или в календаре, не перехватывается — чтобы не мешать их собственной горизонтальной прокрутке.
- **Новый раздел карточки клиента «🧾 Последние продажи»** — по образцу регулярного/тестового ассортимента. Важное честное ограничение: в исходных данных нет номеров и дат отдельных накладных, только помесячные суммы по товару — поэтому раздел показывает **последние 3 месяца, где у клиента были продажи** (не обязательно подряд), с товаром/кол-вом/ценой за штуку и суммой по каждому месяцу. Это ближайший доступный аналог «последних поставок» при текущих данных; если появится выгрузка с реальными номерами/датами документов — раздел можно будет переписать под настоящие накладные.

## Новое 07.09.2026 (Фаза 21): исправлен баг — поиск на странице «Клиенты» терял фокус после каждой буквы

- **Поиск клиентов теперь можно печатать целиком**, не кликая в поле заново после каждой буквы. Причина была в том, что поле поиска перерисовывало всю страницу «Клиенты» на каждое нажатие клавиши, из-за чего браузер терял фокус на пересозданном элементе поля — теперь фокус и позиция курсора возвращаются на поле сразу после перерисовки.
- Второе поле поиска клиента — в форме «Новая задача» (сужает список клиента при создании задачи) — этим багом затронуто не было, там ничего не менялось.

## Новое 08.09.2026 (Фаза 22): продажа — простая задача без этапов, переименование агента, «работают по документам»

- **Задача «Продажа» упрощена** — раньше это была отдельная воронка «Звонок → Встреча → Сделка/Провал» (`SALE_STAGES`) с обязательными пояснением и аудиозаписью встречи перед закрытием. Теперь `taskType: 'sale'` технически устроена **точно так же, как обычный визит**: те же этапы (В работе / Выполнена / Не выполнена / Архив), тот же общий отчёт, обязательный перед переводом в «Выполнена». Отличается только своим набором тегов — **бренды (Kapous/Studio/EPICA/Чистовье/Палитра/AV-ES-PRO-MS) + «Договор»** — и заголовком/вкладкой в интерфейсе, чтобы сохранить отдельную фильтрацию задач по продажам от обычных визитов. Секция аудиозаписи встречи убрана из формы задачи (сервер-эндпоинт оставлен нетронутым, на случай возврата функции). Старые задачи на прежних этапах (`call`/`meeting`/`deal`/`fail`) автоматически переводятся в новую схему при каждом старте сервера (`call`/`meeting` → «В работе», `deal` → «Выполнена», `fail` → «Не выполнена»; уже записанное пояснение переносится в отчёт, если отчёт ещё пуст). Автовыгрузка в Google Таблицу (`src/googleSheets.js`) была завязана на переход в этап «Провал» — теперь срабатывает при переводе продажи в «Не выполнена».
- **Агент «Анастасия» переименован в «Бегимай»** — логин (email `anastasia@cosmedica.local`) и пароль не менялись, только отображаемое имя везде в интерфейсе. Миграция идёт по email (стабильный идентификатор, не зависит от текущего имени в базе) и выполняется **до** обычного импорта данных за один старт сервера — иначе сопоставление владельца клиента по имени внутри импорта не находит переименованную запись и создаёт дубли клиентов под администратором (найдено и исправлено при тестировании на копии уже развёрнутой базы — подробности в статус-документе проекта).
- **Новый признак клиента «работают по документам»** — переиспользует уже существующее поле карточки `contractStatus` («Работает по договору», добавлено в более ранней фазе), а не заводит отдельное поле — по присланному списку `Клиенты по документам.xlsx` (211 строк) это тот же самый концептуальный признак, названный пользователем другими словами. Сопоставление имён из файла с базой клиентов (816 записей) сделано вручную-автоматизированным сравнением (сначала точное вхождение названия, затем пересечение отличительных слов минимум по двум) — **82 клиента сопоставлены уверенно и отмечены «да»** (из них 20 — новые по сравнению с уже стоявшими ранее 84 записями «да»); совпадения на один-единственный общий признак (28 шт.) и вовсе не найденные (69 шт.) намеренно **не отмечены** — при ручной проверке среди них были ложные совпадения на общие слова (название улицы, «больница», «институт», распространённое имя) — решили не гадать. Как и переименование агента, обновление уже существующих карточек идёт отдельной идемпотентной миграцией при каждом старте сервера (обычный импорт не переписывает `contractStatus` у уже созданных клиентов).
- **Загружен срез продаж за 01.09–07.09.2026** (первая неделя сентября, из 6 файлов по агентам, сверено построчно с «Итого» каждого файла — 2303 шт. / 971 147 сом) — по явному выбору пользователя данные загружены в показатели «текущий месяц» сразу, не дожидаясь закрытия полного месяца (это срез за неделю, а не полный сентябрь). При загрузке найден и исправлен архитектурный баг: карточки клиентов, не входящие в файл `agents_clients.json`, не обновлялись **ни одним** циклом импорта и молча хранили бы `currentMonthRevenue`/`currentMonthItems` от какого-то давнего среза сколько угодно долго; раньше это было незаметно, потому что защита от «протухших» данных обнуляла эти поля у всех клиентов в ответах API. Исправлено новой очисткой в конце `runImportBody()` (`src/import.js`): поля обнуляются у любой карточки, которую основной цикл импорта не затронул. Проверено на реальной базе и на чистой установке.
- **Найден и исправлен ещё один баг — старый формат выгрузки задач не загружался обратно.** При проверке загрузки файла `data/tasks-export...json`, выгруженного ДО обновления кода (старые значения этапа продажи — `call`/`meeting`/`deal`/`fail`, ещё старое имя агента), выяснилось: `/api/tasks/import` вставлял такие задачи в базу «как есть», без пересчёта на новую схему этапов — задача успешно попадала в базу, но не появлялась ни в одной колонке доски «Продажи» (колонки знают только актуальные `TASK_STAGES`), то есть выглядело так, будто загрузка ничего не делает. Причина — миграция старых этапов (`migrateLegacyTaskStages()`) применяется только к уже хранящимся в базе задачам при каждом старте сервера, а не к тем, что приходят через `/api/tasks/import` уже после старта. Исправлено — общая логика вынесена в `normalizeLegacyTaskStage()` и теперь применяется в обоих местах: и при старте сервера, и при загрузке файла с задачами. Проверено загрузкой присланного файла (2 задачи-продажи на этапе `call`) — обе корректно попали в «В работе» с правильным клиентом и агентом.

## Новое 08.09.2026 (Фаза 23): бэклог из 10 пунктов — 8 реализовано, 1 закрыт без правок, 1 подтверждён как уже корректный

08.09.2026 пользователь прислал список из 10 пунктов доработки с явной пометкой «зафиксировано со слов пользователя, не приступать без отдельной команды» — список был сначала только записан в документацию проекта, реализация началась по отдельной команде «делай все правки в коде» тем же днём.

- **П.1 — «Продано (Август)» жёстко зашито на август.** При проверке оказалось, что подпись колонки на странице «Клиенты» уже давно динамическая (`latestSalesMonth()`, приходит с сервера через `/api/me`/`salesMonths`) — совпадение с «август» было случайным (это просто последний загруженный месяц на момент, когда пользователь это заметил). Код не менялся, это подтверждено, а не исправлено.
- **П.2 — поиск клиента на странице «Задачи».** Добавлено текстовое поле над досками (по названию клиента/телефону, а для задач без клиента — по названию самой задачи), общее для всех четырёх воронок. Тот же паттерн сохранения курсора при перерисовке, что и у аналогичного поиска на странице «Клиенты» (иначе после каждой буквы приходилось бы кликать в поле заново, этот баг уже когда-то ловили на «Клиентах», см. Фазу 21).
- **П.3 — время визита «от–до».** У задач-визитов было только одно поле времени (`visitTime`). Добавлено второе — `visitTimeTo` (поле `visitTime` сохранено под старым именем ради совместимости со старыми резервными выгрузками задач, теперь означает «от»). В интерфейсе — два поля времени рядом, на карточке задачи — диапазон через тире (если «до» не заполнено, показывается только «от», как раньше).
- **П.4 — смена воронки (типа) у уже созданной задачи.** Раньше `taskType` (визит/продажа/лист ожидания) можно было указать только при создании — ошибку при выборе воронки нельзя было исправить, кроме как удалить задачу и создать заново. Теперь в карточке задачи у администратора/супервайзера есть отдельный выпадающий список «Воронка» — при смене этап и теги, недопустимые в новой воронке, автоматически сбрасываются на дефолт (иначе, например, этап «Товар получен клиентом» листа ожидания оказался бы невалиден у обычного визита). Задачи-агенту (см. п.6) в этот список не входят — у них нет клиента, а обратная смена воронки потребовала бы его указать, поэтому такое преобразование сюда не включено.
- **П.5 — права супервайзера на редактирование любых задач.** При проверке оказалось, что на уровне кода это уже так: и на бэкенде (`isStaff()`), и во фронтенде супервайзер и администратор трактуются одинаково — оба видят и могут редактировать/удалять ЛЮБУЮ задачу, а не только созданную или назначенную на себя (единственное реальное ограничение по ролям — это агент, который видит и правит только свои задачи). Код не менялся, поведение подтверждено, а не исправлено.
- **П.6 — отдельная воронка «Задачи агенту» без привязки к клиенту.** Самый архитектурно нетривиальный пункт бэклога — `clientId` был обязателен почти везде. Добавлен новый `taskType: 'agent'` — такая задача не привязана ни к какому клиенту (`clientId: null`), у неё нет тегов и времени визита, но есть общие этапы (В работе/Выполнена/Не выполнена/Архив), отчёт, вложения и заявка на перенос даты — как у обычной задачи. Новая вкладка «Задачи агенту» на странице «Задачи», своя доска. Работает даже в системе без единого клиента (единственный тип задачи, для которого это возможно). Резервная выгрузка/загрузка задач (`/api/tasks/export`/`import`) тоже обновлены — иначе такие задачи при восстановлении из файла помечались бы «клиент не найден» и терялись.
- **П.7 — фильтр «на согласовании» на странице «Клиенты».** Уточнение пользователя: речь про уже существующий флоу (`pendingApproval`, ставится при создании клиента агентом, кнопка «Одобрить точку» у супервайзера/администратора) — не хватало только фильтра, чтобы быстро отсеять такие карточки, не разглядывая бейджи по всему списку. Добавлен булев чекбокс-фильтр «На согласовании» (виден администратору/супервайзеру) по образцу уже существующих «Есть задолженность»/«Не добрал».
- **П.8 — фильтр по переносу даты в задачах.** Добавлен чекбокс «Есть заявка на перенос даты» (поверх уже существующего поля `dateChangeRequest`), общий для всех четырёх воронок задач. Заодно бейдж «заявка на перенос даты» на карточке задачи (раньше был только на доске «Продажи») теперь показывается на всех досках одинаково.
- **П.9 — загрузка новых клиентов + пересчёт продаж — снято с очереди без изменений в коде.** Пользователь уточнил: он продолжит присылать заранее выгруженный список клиентов для пересборки через сессию с Claude — текущий процесс (`agents_clients.json` готовится вручную перед каждой пересборкой) устраивает, автоматическое подхватывание новых клиентов делать не нужно.
- **П.10 — акции только из последнего загруженного реестра.** При проверке оказалось, что `promotions` у клиента и так полностью пересчитывается заново при каждом импорте из одного файла `data/import/promotions.json` (не накапливается и не мёржится со старыми данными), да ещё и обнуляется наравне с продажами текущего месяца, если срез «протух» (см. `CURRENT_MONTH_DATA_STALE_FROM`). Архитектурно это уже устроено так, как просил пользователь — код не менялся. Если на практике всё же встретится случай, когда показываются акции из более старого реестра — это отдельный, ещё не описанный случай, для его разбора понадобятся детали конкретного наблюдения.

## Новое 09.09.2026 (Фаза 24): подпись «Продано (Август)» → «Продано в этом месяце»; смена воронки задачи разрешена агентам без обращения к супервайзеру

Подписи с названием месяца, подставляемым динамически через `latestSalesMonth()` (например «Продано (Август)»), заменены на статичный текст «продано в этом месяце» — без названия месяца — в двух местах, явно указанных пользователем:
- Дашборд, панель «Мои показатели» (видна агенту) — карточка суммы продаж текущего месяца: было «Продано (Август)», стало «Продано в этом месяце».
- Карточка контрагента (клиента), поле «План продаж (мес.)» — было «... (факт за Август: N)», стало «... (продано в этом месяце: N)».

Другие подписи с названием месяца в интерфейсе (отчёт «Ассортимент по агентам», топ-10 по брендам, панель эффективности супервайзера) пока не трогались — пользователь указывал места по одному, по мере того как замечал их; если понадобится ещё где-то, править по тому же образцу (искать `latestSalesMonth()` в `public/app.js`).

**09.09.2026, отдельным сообщением:** пользователь попросил заменить подпись и в заголовке колонки таблицы «Клиенты» — было «Продано (Август)» (динамически, через `latestSalesMonth()`), стало статичное «Продано в этом месяце» (без названия месяца, как и два места выше). Сама сортировка по этой колонке (`data-sort="sales"`, добавлена этой же Фазой 24, см. ниже) не пострадала — менялся только текст заголовка `<th>`.

Отдельно, тем же сообщением-потоком: пользователь сообщил, что агент не смог сменить воронку (тип) задачи, и попросил разрешить это каждому агенту напрямую, без обращения к супервайзеру/администратору. Раньше выпадающий список «Воронка (тип задачи)» в карточке задачи (введён в Фазе 23, п.4) был доступен только администратору/супервайзеру — теперь доступен любому, у кого вообще есть право редактировать эту задачу (свою — агенту, любую — админу/супервайзеру, как и раньше). Ограничение снято и на бэкенде (`PUT /api/tasks/:id`, `src/api.js`), и на фронтенде (`openTaskModal`, `public/app.js`) — граница прав не пострадала: агент по-прежнему не может редактировать чужую задачу (проверено отдельным тестом — второй агент получает «Недостаточно прав» при попытке сменить воронку чужой задачи).

Ещё три правки тем же коротким потоком сообщений:
- **Убран «Остаток» (запас на складе) везде в интерфейсе** — пользователь написал «убери остаток кол-во везде где он есть он больше не нужен». Убран бейдж 📦 на позициях ассортимента (`stockBadgeHtml()` теперь всегда возвращает пустую строку), кнопка «⬆ Загрузить остатки» и её обработчик на странице «Команда», backend-эндпоинт `POST /api/stock/import`, поле `stockAsOf` из ответа `/api/me`. Импорт (`attachStock()` в `src/import.js`) по-прежнему подмешивает `stockQty`/`stockUnit` в данные при обычной пересборке — это не трогали (не показывалось бы всё равно), проще было оставить как есть, чем менять сигнатуры `computeAssortment()`/`computeTestAssortment()`.
- **Убраны «Акции» по клиентам целиком** — пользователь написал «убери акции по клиентам текущие показатели этого месяца» (акции и так были устроены как срез текущего месяца, гаснущий по той же логике, что продажи текущего месяца — см. Фазу 23 п.10). Убрано: бейдж «🎁 акции» и фильтр «Есть акции»/«Акция: любая» на «Клиентах», раздел «🎁 Акции» в карточке клиента, карточка «Клиентов с акциями» на дашборде агента, панель «🎁 Акции (вся команда)» на дашборде супервайзера, весь отчёт «Акции» на странице «Отчёты», backend (`GET /api/reports/promotions`, `promotionsSummary` в `/api/stats`, `promotionsClientsCount`/`promotionsItemsCount`/`promotionsSumTotal` в дашборде агента). `withFreshCurrentMonth()` (`src/api.js`) теперь всегда отдаёт `promotions: []` — единая точка, где фича выключена для всего API разом.
- **Сортировка клиентов по продажам за сентябрь** — колонка «Продано» в таблице «Клиенты» стала кликабельной (`data-sort="sales"`, сортирует по `currentMonthRevenue`) — по тому же паттерну, что уже были «Долг»/«Недопродано»/«Маршрут №». «Сентябрь» здесь — это и есть «текущий месяц» (`currentMonthRevenue`); заголовок колонки при этом стал статичным текстом «Продано в этом месяце» отдельной правкой того же дня (см. выше) — больше не подставляет название месяца через `latestSalesMonth()`.

## Обновление 09.09.2026 (Фаза 25): точечная догрузка продаж по двум агентам (Нагима, Татьяна) из присланных .xls-отчётов

Пользователь прислал два файла «Анализ <Агент>.xls» (выгрузка продаж за 01.09–08.09.26 по одному конкретному агенту — Нагима, затем Татьяна) с прямой просьбой: обновить продажи ТОЛЬКО этого агента, остальных не трогать, и перед началом проверить соответствие клиентов и общую сумму. Это не правка кода — это разовое обновление данных `data/import/*.json`, но оно потребовало много проверок и решений, поэтому задокументировано подробно.

**Важное техническое открытие по ходу работы**: `data/db.json` — в `.gitignore`, никогда не попадает на GitHub/Render. Реальный сервер пользователя держит СВОЮ базу, которая при каждом запуске (`server.js` → `runImport()`) пересобирается/обновляется ИЗ файлов `data/import/*.json` — упорно править саму базу в рабочей копии сессии бессмысленно для доставки: любое изменение, которое должно пережить деплой, обязано попасть в `data/import/*.json` (или в код), а не только в `data/db.json`. Это стоило одной ошибки по ходу работы (см. ниже про карточку-корректировку) — исправлено, но важно помнить на будущее.

**Разбор .xls**: формат — построчный экспорт «Контрагент → товар → строка "Реализация NNNN (дата)"», с итоговой строкой «Итого». Написан разовый парсер (Python, LibreOffice для конвертации .xls→.xlsx, pandas для чтения) — клиентские блоки распознаются по правилу «после клиента всегда идёт товарная строка, после товарной строки всегда идёт "Реализация"» (без этого правила клиента от товара по одному только присутствию адреса не отличить надёжно). Бренд/категория товара определяются тем же правилом, что и раньше (см. Фазу 6.1/22): префиксы `E `/`HY `/`S `/`MS `/`AV `/`ES `/`PRO `→ EPICA/Kapous/Studio/AV-ES-PRO-MS, слово «Палитра» в начале → Палитра, оставшаяся латиница без пробела после буквы (напр. «EStep») → Прочее, всё остальное → Чистовье; категория «Краситель»/«Оксид» — по ключевым словам в названии. Итоговая сумма/кол-во из парсера сверялась с строкой «Итого» в каждом файле — совпало в обоих случаях (Нагима: 426 шт / ₸275 188; Татьяна: 1063 шт / ₸429 883) и на постатейном совпадении «сумма клиента = сумма его товарных строк».

**Нагима (₸275 188, 24 клиента)**: 18 клиентов нашлись в базе однозначно (по имени + владельцу). 5 клиентов не нашлись вообще ни у одного агента — по явному запросу пользователя добавлены как новые клиенты Нагимы (адрес — из отчёта). Ещё 1 клиент — «Novotel Bishkek ИнтеграХотел Инвестмент» (₸8 025) — уже существовал в базе, но за агентом Татьяна, причём с ТОЧНО такими же суммой/товарами (тот же самый визит, просто попавший в оба отчёта). Пользователь подтвердил: переназначить на Нагиму.

**Татьяна (₸429 883, 48 клиентов)**: 35 клиентов нашлись однозначно, 1 («Азбука тела Низ») тоже совпал по имени, но с карточкой Альбины (id 253) — тот же адрес, та же сумма, уже верно; трогать общий файл `current_month_sales.json` не стали (задело бы Альбину), карточка Татьяны и так была верна. 1 клиент («МЦ Меди Ко», ₸1 800) не найден нигде — добавлен как новый клиент Татьяны. **10 клиентов (₸49 673)** — Natural Beauty, Nel clinic, Звезда салон красоты, Лайф стайл, МЦ Unimed Clinic, Салон красоты Барыш/Версаль/Эксперт, Стоматология Denta Tech/Alef — оказались уже в базе за агентом Бегимай, причём АДРЕС/СУММА/ТОВАРЫ совпали тютелька в тютельку у всех десяти. Пользователь подтвердил: карточки остаются у Бегимай (не переназначать), но фактическую отгрузку по ним делала Татьяна, пока эти точки не перешли к Бегимай — попросил включить эту сумму в ОБЩИЙ ИТОГ продаж Татьяны за период, при этом карточки клиентов (и, соответственно, итог Бегимай) не трогать. Технически для этого добавлена карточка-корректировка «Корректировка отгрузки Татьяны (клиенты закреплены за Бегимай)» (₸49 673, `isOffRoute: true`, явно описана в поле «Адрес» как техническая, не настоящая точка) — со стороны Татьяны в её «Продано в этом месяце» сумма учтена; со стороны Бегимай ничего не менялось. **Осознанный побочный эффект**: в общекомандной сумме на дашборде супервайзера (`salesPerformance.actualTotal`, суммирует ВСЕХ клиентов без учёта агента) эти ₸49 673 считаются дважды — один раз на карточках Бегимай, второй раз на карточке-корректировке Татьяны. Пользователь предупреждён и явно согласился («у бегимай будет свой пересчёт» — то есть при следующей полной сверке данных по Бегимай это разберётся отдельно).

**«Частное лицо»** — отдельная давно известная проблема (см. «Известные ограничения» ниже и Фазу 22): это общее название для клиентов без опознанного контрагента, и в базе под ним отдельные карточки сразу у 4 агентов (Батаева/Альбина/Бегимай/Татьяна), а `current_month_sales.json` матчит текущий месяц ТОЛЬКО по имени клиента, без учёта агента — значит, одно число в файле уходит сразу всем четверым при пересборке. Отчёт Татьяны показывал её «Частное лицо» как ₸3 012, но в базе у всех четверых уже стояла общая сумма ₸12 012. Пользователь решил: переименовать карточку Татьяны в «Частное лицо (Татьяна)» — так у неё в `agents_clients.json`/`current_month_sales.json` появился свой отдельный ключ по имени, не пересекающийся с остальными тремя агентами. Технический побочный эффект переименования: старая карточка «Частное лицо» (id 684, Татьяна) больше не сопоставляется ни с одной строкой `agents_clients.json` под её именем — при пересборке она «осиротеет» и её `currentMonthRevenue`/`currentMonthItems` обнулятся (это штатное поведение той же самой «очистки осиротевших карточек», что описана в Фазе 22) — сама карточка не удаляется, просто теперь пустая; агент увидит новую карточку «Частное лицо (Татьяна)» рядом со старой пустой «Частное лицо» в своём списке клиентов.

**Проверка перед пересборкой**: главный урок этого раунда — при обновлении текущего месяца по ОДНОМУ агенту нельзя просто найти совпадения по имени в пределах его собственных клиентов и остановиться: нужно ещё раз проверить эти же имена по ВСЕЙ базе (across all agents), потому что общий файл `current_month_sales.json` матчит по имени без учёта агента — про это уже было известно для «Частное лицо» (Фаза 22), но в этом раунде та же ловушка нашлась ещё в двух местах (Novotel/Татьяна и 10 клиентов/Бегимай), которые не были бы пойманы, если бы проверялось только «нашёлся ли клиент у ЭТОГО агента». Итоговая сумма (`currentMonthRevenue` по всем клиентам агента) после каждого шага сверялась со строкой «Итого» файла — сошлось у обоих агентов: ₸275 188 (Нагима) и ₸429 883 (Татьяна). Проверено также на «чистой установке» (копия без `data/db.json`, только с обновлёнными `data/import/*.json`) — и Нагима, и Татьяна, и Бегимай (её карточки не тронуты), и Альбина (Азбука тела Низ не тронута) показывают правильные суммы, что подтверждает: изменения корректно доедут до реального сервера пользователя при следующем деплое.

## Обновление 11.09.2026 (Фаза 26): поиск клиента в форме «Встреча с клиентом» (Календарь)

**Жалоба пользователя**: «если выбирать клиента в задачах то нет подбора по имени».

**Проверено и не подтвердилось**: в модалке «+ Новая задача» (раздел «Задачи») поиск клиента по названию уже есть и работает — проверено вживую на тестовом сервере (Playwright) и за агента, и за администратора: список из сотен клиентов корректно сужается по подстроке. При редактировании существующей задачи клиент намеренно заблокирован (менять его нельзя) — там поля поиска нет по замыслу, это не баг.

**Что оказалось багом на самом деле**: в разделе «Календарь» у супервайзера есть отдельная форма «+ Встреча с клиентом» — там был обычный `<select>` со всеми клиентами базы (692 варианта) единым списком, без какого-либо поиска. Пользователь уточнил (через уточняющий вопрос), что речь именно об этой форме.

**Исправление**: код поиска-по-подстроке над `<select>`, ранее жёстко привязанный к форме задачи (`wireTaskClientSearch`), вынесен в переиспользуемую функцию `wireClientSearchSelect(searchInputId, selectId, clients)` (`public/app.js`). Форма «Встреча с клиентом» (`openSupervisorMeetingModal`) получила такое же текстовое поле `#sup-meeting-client-search` над `<select id="sup-meeting-client-select">`, использующее ту же функцию. Логика не изменилась: `<select>` остаётся источником истины при отправке формы, поле поиска только сужает видимый список опций по подстроке в названии (без учёта регистра).

**Проверка**: на тестовом сервере (чистая копия без `data/db.json`) — вход супервайзером (`alexandr@cosmedica.local`), «Календарь» → «+ Встреча с клиентом»: список из 692 клиентов, после ввода «Азбука» — сужается до 3 вариантов («Азбука тела», «Азбука тела Низ» ×2 у разных агентов). Форма «Новая задача» перепроверена после рефакторинга — поиск по-прежнему работает (692→3 варианта на «Азбука» за администратора, 123→2 варианта на «Pro» за агента Нагиму). `node --check public/app.js` — без ошибок.

## Обновление 11.09.2026 (Фаза 27): в «Недопродано» не путать с тем, что уже купили — бейдж «оформлено»

**Просьба пользователя**: «в недопроданно указывать позиции которые не попали в реализацию то есть если они есть в регулярном ассортименте и их купили считать их не надо а списке подсветить меткой оформлено».

**Разбор**: сама логика подсчёта «Недопродано» уже делала ровно то, что описано первой половиной фразы — `computeAtRisk()` (`src/api.js`) помечает позицию регулярного ассортимента `atRisk: false`, если она уже встречается в продажах текущего месяца (`currentMonthItems`), и только позиции с `atRisk: true` попадают под заголовок «⚠️ Недопродано» и в счётчик `riskCount()`/бейдж «N недопродано» в таблице «Клиенты» — уже купленные в этом месяце позиции туда не идут и не менялось. Это подтверждает и старая формулировка в известных ограничениях (Фаза 18): «Недопродано» = постоянный ассортимент минус то, что уже куплено в текущем месяце.

Второй частью просьбы — «а в списке подсветить меткой оформлено» — оказалось реальное изменение: в самом списке «Регулярный ассортимент» на карточке клиента уже купленные в этом месяце позиции показывались обычными строками наравне с остальными, никак не отмеченными, хотя факт «уже оформлено» полезно видеть сразу, не открывая отдельно «Последние продажи».

**Исправление**: `assortRow()` (`public/app.js`) — единая функция рендера строки ассортимента, используемая и для обычных позиций регулярного ассортимента, и для «Недопродано», и для тестового ассортимента — теперь добавляет бейдж «оформлено» (зелёный, стиль `.badge-yes`, как у прочих утвердительных пометок в проекте) рядом с названием товара, когда `p.atRisk === false`. У тестового ассортимента поля `atRisk` нет вообще (`undefined`), поэтому там бейдж не появляется — это осознанно: понятие «недопродано»/«оформлено» относится только к регулярному ассортименту. Считать/фильтровать ничего заново не пришлось — `computeAtRisk()`/`riskCount()` не менялись, бейдж чисто визуальный поверх уже верной логики.

**Проверка**: на тестовом сервере (чистая установка без `data/db.json`) найден через API клиент со смешанным набором (id 30, «МЦ Родильный дом» — 2 позиции уже куплены в этом месяце, 2 ещё нет), открыта его карточка → «Регулярный ассортимент» через Playwright: бейдж «оформлено» показан ровно у 2 позиций, заголовок «⚠️ Недопродано — ещё не покупали в текущем месяце» по-прежнему показан и содержит только оставшиеся 2. `node --check public/app.js` — без ошибок.

## Обновление 11.09.2026 (Фаза 28): клиенты, заведённые агентом самостоятельно, больше не теряются при пересборке

**Вопрос пользователя**: «как нам быть с клиентами которых агенты завели сами они выпадают из общего списка при пересборки посоветуй как решить эту задачу».

**Диагноз**: это архитектурный, а не косметический баг — нашёлся не сразу, потребовал разбора всей цепочки импорта. Карточка, которую агент завёл сам через интерфейс (кнопка «+ Клиент», согласование супервайзером), не входит в `data/import/agents_clients.json` — единственный файл, из которого основной цикл импорта (`runImportBody()` в `src/import.js`) создаёт/обновляет клиентов при каждой пересборке. Пока сервер просто перезапускается на уже существующей базе (`data/db.json` на диске не стёрт) — такая карточка не теряется целиком, только «осиротевшую» помечает и обнуляет ей `currentMonthRevenue`/`currentMonthItems` (см. Фазу 22). Но при НАСТОЯЩЕЙ пересборке — новая сессия с Claude, новый архив, `data/db.json` намеренно не входит в архив (`.gitignore`, см. Фазу 25) — сервер стартует с базой, полностью пересозданной ИЗ `agents_clients.json`, и такой карточки там нет вообще: её не существовало на исходных маршрутах, потому что агент завёл её сам, уже после того как маршруты были собраны. Единственный штатный механизм переноса ручных данных между пересборками — кнопки «Выгрузить клиентов»/«Загрузить клиентов» (страница «Команда», Фаза 17) — но до этой правки «Загрузить клиентов» только ОБНОВЛЯЛ уже существующих клиентов по имени+агенту, а тех, кого не находил (`/api/clients/import`, `src/api.js`), просто пропускал в `unresolved` — то есть самостоятельно заведённые клиенты выгружались в файл корректно (`/api/clients/export` берёт вообще всех клиентов из базы), но при обратной загрузке терялись насовсем.

**Предложил пользователю 3 варианта решения** (через уточняющий вопрос) — (1) починить кнопку «Загрузить клиентов», чтобы недостающих не пропускала, а создавала заново — минимальная правка, использует уже привычный процесс «выгрузили → пересобрали → загрузили»; (2) то же самое плюс: в начале каждой будущей сессии пересборки Claude сам спрашивает у пользователя свежий файл экспорта и включает его в архив, чтобы не нужно было помнить про кнопку «Загрузить» после; (3) структурно — чтобы одобренная супервайзером новая точка сразу дописывалась в `agents_clients.json` на сервере, тогда никакие кнопки вообще не нужны, но это сложнее и зависит от того, что диск на Render не эфемерный. **Пользователь выбрал вариант (1).**

**Реализация**: `/api/clients/import` (`src/api.js`) — если клиент с таким именем у этого агента не найден в текущей базе, вместо пропуска в `unresolved` теперь создаётся новая карточка (`db.insert`) с теми же дефолтами, что и при обычном создании клиента агентом (`POST /api/clients`), плюс ручные поля из выгрузки поверх дефолтов. `pendingApproval: false` — эта кнопка доступна только администратору (`requireAdmin`), восстановление уже одобренной ранее карточки не должно требовать повторного согласования супервайзером заново. Единственное, что теперь реально остаётся в `unresolved`, — агент, которого в текущей базе вообще нет (переименован/удалён), а не «клиент не найден». Ответ эндпоинта дополнен полем `created` (счётчик пересозданных), фронтенд (`renderTeam()`, `public/app.js`) показывает его в алерте после загрузки и обновил подсказки у обеих кнопок.

**Проверка** (не только словами — полный сквозной прогон через API на тестовом сервере): 1) вошёл агентом, создал через `POST /api/clients` тестовую самостоятельную точку («Тестовая точка агента Фаза28», `pendingApproval: true`); 2) вошёл администратором, `GET /api/clients/export` — клиент присутствует в выгрузке; 3) остановил сервер, удалил `data/db.json`, запустил заново на чистом порту — полная симуляция «пересборки с нуля»: клиента снова 692 (базовые), тестовая точка отсутствует; 4) `POST /api/clients/import` с ранее выгруженным файлом → ответ `{updated: 692, created: 1, unresolvedCount: 0}`; 5) карточка на месте: правильный `ownerId` (агент), адрес/телефон сохранены, `pendingApproval: false`. Повторный вызов того же импорта — `{updated: 693, created: 0}`, дублей не появилось (идемпотентно). `node --check src/api.js public/app.js` — без ошибок.

**Важно для пользователя на будущее**: этот фикс работает только вместе с уже существующей дисциплиной — «Выгрузить клиентов» нужно нажимать ПЕРЕД тем, как отдать данные для новой пересборки (то есть до начала новой сессии с Claude), а «Загрузить клиентов» — после того, как новый архив задеплоен. Если экспорт не сделан заранее — восстанавливать нечего, самостоятельно заведённые клиенты будут потеряны, как и раньше. Отдельно стоит помнить: у восстановленных таким способом клиентов ассортимент/долг/продажи текущего месяца останутся пустыми, пока они не появятся в `agents_clients.json`/файлах продаж на одной из будущих пересборок — это ручные поля восстанавливаются полностью, а расчётные — как обычно, из отдельных источников.

## Обновление 14.09.2026 (Фаза 29): фильтр «Просроченные» на странице «Задачи»

**Просьба пользователя**: «в задачах нет фильтра просроченные добавь запиши».

**Реализация**: добавлен чекбокс «Просроченные» в общую панель фильтров страницы «Задачи» (`public/app.js`, `renderTasks()`) — рядом с уже существующим «Есть заявка на перенос даты», общий для всех четырёх воронок (продажи/визиты/лист ожидания/задачи агенту), по тому же паттерну (`state.onlyOverdue`, обработчик `change` → `render()`).

Критерий «просрочено» не придуман заново — он уже использовался поштучно (бейдж «просрочено» на карточке задачи) на всех четырёх досках и в счётчике «Просроченных задач» на дашборде, но каждый раз считался отдельным инлайн-выражением: `t.dueDate && t.dueDate < today && ACTIVE_STAGES.includes(t.stage)` (визиты/продажи/задачи агенту) или `... && stage.key !== 'received'` (лист ожидания). Вынесено в одну функцию `isTaskOverdue(t)` (использует уже существующую `isTaskActiveClient(t)`, которая ровно так же различает воронки) и подставлено во все 4 места, где раньше было дублирование, плюс в новый фильтр — так что чекбокс «Просроченные» гарантированно считает так же, как уже видимые на карточках бейджи и как дашборд, а не по своей отдельной логике.

**Проверка**: на тестовом сервере (чистая установка без `data/db.json`) созданы через API две тестовые задачи одному и тому же агенту — с датой в прошлом (`2020-01-01`) и в будущем (`2099-01-01`); на доске «Воронка продаж» видны обе, после включения «Просроченные» — остаётся только просроченная, снятие галочки возвращает обе. Переключение между досками не сбрасывает состояние чекбокса. `node --check public/app.js` — без ошибок.

## Обновление 14.09.2026 (Фаза 30): фильтр «На согласовании» на «Клиентах» теперь ловит и заявки на закрытие

**Жалоба пользователя**: «не работает фильтр на согласование должны туда поподать все на удаление на добавление всё что нужно согласовывать супервайзеру».

**Диагноз**: фильтр не был сломан механически — он честно фильтровал именно то, что проверял в коде, но проверял слишком узко. `onlyPendingApproval` (`public/app.js`) смотрел только на `c.pendingApproval` — флаг «новая точка от агента ждёт согласования» (Фаза 17/23). У точек с запросом на закрытие («на удаление», в терминах пользователя) — отдельный флаг `c.closureRequested` (см. `POST /api/clients/:id/request-closure`, `src/api.js`), у которого уже есть свой собственный бейдж «на закрытие» в списке клиентов и своя пара кнопок подтверждения/отклонения в карточке, но под фильтр он не подпадал. На сервере оба состояния уже считались как единое понятие «на утверждении у супервайзера» — эндпоинт статистики отдаёт `pendingApprovalCount` и `pendingClosureCount` отдельными числами и складывает их в общий счётчик, — просто фронтенд-фильтр эту связь не учитывал.

**Реализация**: условие фильтра расширено до `f.onlyPendingApproval && !(c.pendingApproval || c.closureRequested)` — один чекбокс «На согласовании» теперь показывает и новые точки, ожидающие подтверждения («на добавление»), и точки с запросом на закрытие («на удаление»), ровно как просил пользователь. Название чекбокса не менялось (оно уже было достаточно общим), добавлена только всплывающая подсказка (`title`), поясняющая оба случая. Других состояний, требующих согласования супервайзера на странице «Клиенты», в коде не нашлось — заявки на перенос даты у задач учитываются отдельным фильтром на странице «Задачи» (Фаза 23), это другая сущность (задача, не клиент), сюда не относится.

**Проверка**: на тестовом сервере (чистая установка без `data/db.json`) через API создана точка с `pendingApproval: true` (агент сам завёл клиента) и отдельно отправлен запрос на закрытие существующей точки (`closureRequested: true`). В интерфейсе супервайзера включение «На согласовании» на «Клиентах» показало обе карточки (693 → 2 строки), с корректными бейджами «на согласовании» и «на закрытие» соответственно. `node --check public/app.js` — без ошибок.

## Обновление 14.09.2026 (Фаза 31): фильтр по воронкам в Календаре

**Просьба пользователя**: «в календарь супервайзера добавь фильтр по воронкам запиши».

**Реализация**: в разделе «Календарь» (все виды — месяц/неделя/день, и «Печать маршрута») добавлена строка из 4 чекбоксов — по одному на каждую воронку задач («Визиты с супервайзером», «Воронка продаж», «Лист ожидания», «Задачи агенту»), с подписями, дословно совпадающими с одноимёнными кнопками-переключателями на странице «Задачи». По умолчанию показаны все воронки (как и раньше — поведение не изменилось, пока фильтр не тронут). Снятая галочка скрывает задачи этой воронки во всех представлениях сразу — единая точка фильтрации `tasksOnDate()` (`public/app.js`), от которой зависят месяц/неделя/день и печать маршрута, поэтому скрытая воронка не «протекает» ни в одно из них по отдельности. Появляется кнопка «Сбросить», когда хотя бы одна воронка скрыта. Встречи супервайзера (отдельная сущность, не задача) фильтром не затронуты — показываются всегда, как и раньше. Фильтр виден на календаре всем ролям (агент тоже видит свои задачи по нескольким воронкам сразу и может сузить список так же, как супервайзер).

**Проверка**: на тестовом сервере через Playwright — зашли супервайзером в «Календарь», нашли 4 чекбокса, сняли 3 из 4 («Воронка продаж», «Лист ожидания», «Задачи агенту»), появилась кнопка «Сбросить»; нажатие «Сбросить» вернуло все 4 в отмеченное состояние. Отдельно — создана через API тестовая задача воронки «Продажа» с сегодняшней датой, в дневном виде календаря она видна («Only nails В работе»); после снятия галочки «Воронка продаж» карточка пропала и день показал «На этот день задач нет.», после повторной установки галочки — снова появилась. `node --check public/app.js` — без ошибок.

## Обновление 14.09.2026 (Фаза 32): новый срез продаж за 01–14.09.26 по всем 6 агентам + исправлена подпись «Август» на карточках с текущими данными

**Просьба пользователя**: прислал 6 файлов «Анализ <Агент>.xls» («новый срез по агентам на 14.09») + «переделай в все текущие продажи назови продажи текущий месяц а не август как сейчас».

**Часть 1 — обновление данных.** Каждый файл — «Продажи за 01.09.26 – 14.09.26» по одному агенту (структура та же, что в Фазе 22/25: клиент бол­дом с итогом, под ним строки товаров, под каждым товаром — служебная строка «Реализация NNNN (дата)», которую при разборе пропускаем, чтобы не задвоить сумму). Сумма и количество по каждому файлу сверены со строкой «Итого» — совпало один в один по всем 6 файлам (Батаева ₸222 932, Альбина ₸277 807, Бегимай ₸127 048, Жанара ₸661 087, Нагима ₸405 130, Татьяна ₸672 155 — суммарно ₸2 366 159 за команду). Бренд/категория товара определялись по уже накопленному словарю «название → бренд/категория» (из `regular_assortment.json`/`test_assortment.json`/предыдущего среза — 1390 позиций, покрытие 99.5%), для 7 нераспознанных позиций — тем же префиксным правилом, что и в остальном проекте (см. «Бренды» в контекст-брифе).

При сверке имён по всей базе (тот же урок, что в Фазе 25 — сопоставление текущих продаж идёт ТОЛЬКО по имени клиента, без агента) обнаружено и обработано:
- **Общее имя «Частное лицо» — теперь у 5 из 6 агентов** (было решено только для Татьяны в Фазе 25). Раньше карточки Батаевой/Альбины/Бегимай были без разбора все названы одинаково «Частное лицо» — при одном общем ключе в `current_month_sales.json` все три получили бы ОДНО и то же (чужое) число. Карточки переименованы в `data/import/agents_clients.json`: «Частное лицо (Батаева)», «Частное лицо (Альбина)», «Частное лицо (Бегимай)» — по тому же образцу, что уже применялся к Татьяне. У Нагимы такой карточки не было вовсе (её «Частное лицо» не встречалось в исходном `agents_clients.json`) — добавлена новая «Частное лицо (Нагима)».
- **«OcOO "DSCA"» (файл Татьяны, латиницей O/c) — тот же самый клиент, что уже есть в базе как «ОсОО DSCA»** (кириллицей, тот же адрес ул.Рыскулова,10, тот же агент) — типографский разнобой латиницы/кириллицы в сокращении организационной формы, не новый клиент. Сумма отнесена на существующую карточку, дубль не создан.
- **«Звезда салон красоты» — один и тот же адрес (ул.Боконбаева,14/Шопокова) продан в этом периоде и Бегимай (₸1 575), и Татьяной (₸8 500)** — карточка в базе одна, принадлежит Бегимай; суммы объединены на неё (₸10 075), по тому же принципу «тютелька в тютельку», что и в Фазе 25.
- **17 клиентов из файлов не нашлись в `agents_clients.json` вообще** (например «Nur Cosmetology», «Кингс клиник», «Метадент», «Чакра Спа») — для 16 из них не нашлось даже близкого совпадения по названию среди клиентов ТОГО ЖЕ агента (проверено `difflib` — не опечатка/вариант написания уже существующей карточки), добавлены как новые карточки в `agents_clients.json` (имя + агент + адрес из файла, остальные поля пустые — как у прочих минимальных карточек). Один случай («OcOO "DSCA"» у Татьяны) — см. выше, это опечатка существующей карточки, новую не заводили.

**Не тронуто (сознательно) — два случая, где после этого среза ОДНО имя всё ещё совпадает между ДВУМЯ разными реальными точками** (не «псевдоним» вроде «Частное лицо», а конкретное совпадение названия):
- **«Азбука тела Низ»** — карточки есть и у Татьяны, и у Альбины, тот же адрес (ул.Логвиненко,19) — известный случай, зафиксированный ещё в Фазе 25 («значение уже было верным, файл не трогали»). В этот раз продажу отразила только Татьяна (₸9 930), но при отдаче клиенту сервер сопоставляет по имени без агента — карточка Альбины тоже покажет эти ₸9 930, хотя реально не продавала. Это тот самый заранее известный и задокументированный случай («Два случая совпадения имени... если это разные точки, нужно ручное решение пользователя» — известные ограничения), решение по нему пользователем ещё не принято, поэтому карточки не переименовывал и не трогал.
- **«Студия Vivienne Lashes» — НОВЫЙ, ранее не встречавшийся случай**: у Альбины (ул.Исанова 79, 306 каб.) и у Бегимай (10 мкр, д.34) — это два РАЗНЫХ адреса, то есть два разных салона с одинаковым названием (сеть/франшиза), а не дубль одной точки. Продала в этом периоде только Альбина (₸2 110), но карточка Бегимай тоже покажет эту сумму по той же причине. Добавлено в список «ждёт решения пользователя» ниже — нужно подтверждение, что это правда разные точки (тогда одну из карточек нужно переименовать для разделения, например «Студия Vivienne Lashes (Исанова)»/«Студия Vivienne Lashes (10 мкр)»), прежде чем менять что-либо в данных.

Из-за этих двух совпадений агрегированная сумма по всей команде в интерфейсе (например, карточка «Факт» на панели «Выполнение плана») на ₸12 040 больше, чем реальная сумма «Итого» по всем 6 файлам (₸2 366 159) — это тот же осознанно принятый компромисс, что и в Фазе 25, пока оба случая не разрешены пользователем.

**Часть 2 — подпись «Август» на карточках, которые на самом деле показывают текущие данные.** При разборе просьбы нашёлся конкретный баг, а не общая переформулировка: 4 места в интерфейсе — кнопка «Топ-10 по брендам» и заголовок таблицы «Выручка» на дашборде агента, карточки «План»/«Факт» на панели супервайзера «Выполнение плана» — были подписаны через `latestSalesMonth()` (последний месяц ОТДЕЛЬНОЙ 6-месячной истории ассортимента, сейчас «август», не обновляется этим срезом), хотя сами цифры на этих карточках считаются из СОВСЕМ ДРУГОГО, уже живого источника — `currentMonthRevenue`/`currentMonthItems` (`data/import/current_month_sales.json`, тот самый файл, который только что обновлён этим срезом). То есть карточки с полностью актуальными сентябрьскими цифрами были подписаны «(Август)» — вводящая в заблуждение подпись, а не просто устаревшая. Функция `latestSalesMonth()` удалена (использовалась только в этих 4 местах), подписи заменены на статичное «(текущий месяц)». Функция-предупреждение о «протухших» данных (`state.currentMonthDataFresh`, баннер наверху дашборда) не затронута — она использует другой, отдельный источник (`state.latestSalesMonth` с сервера) и осталась как была, это два разных, не связанных между собой понятия «последний месяц».

**Проверка**: на тестовом сервере (чистая установка без `data/db.json`) — импорт отработал (709 новых контрагентов + обновлено 1), сумма `currentMonthRevenue` по всем клиентам = ₸2 378 199 (= ₸2 366 159 «чистой» суммы + ₸12 040 от двух известных совпадений выше — сошлось до рубля). Проверены поимённо все переименованные/новые карточки (`Частное лицо (Батаева/Альбина/Бегимай/Нагима/Татьяна)`, `Звезда салон красоты` = ₸10 075, `ОсОО DSCA` = ₸4 000, `Чакра Спа` = ₸42 309). На дашборде агента Нагимы «Продано в этом месяце: 405 130 сом» — совпадает с «Итого» её файла один в один. Через Playwright подтверждено: подписи «Топ-10 по брендам (текущий месяц)», «Выручка (текущий месяц)», «План (текущий месяц)», «Факт (текущий месяц)» — нигде больше не встречается «Август» вне легитимных мест (чипы-фильтр 6-месячной истории Февраль…Август, которые физически привязаны к другому, немодифицированному источнику данных). `node --check` — без ошибок на всех трёх изменённых файлах.

**Что ждёт решения пользователя**: ~~подтвердить, что «Студия Vivienne Lashes» у Альбины и Бегимай — это разные салоны~~ — **подтверждено пользователем 14.09.2026: это разные точки.** Карточки пока НЕ разнесены (пользователь попросил «запомни но не переделывай» — зафиксировать факт, саму правку в `agents_clients.json`/`current_month_sales.json` отложить на будущее, отдельным шагом). И отдельно ещё не решён давний вопрос по «Азбука тела Низ» (Татьяна/Альбина, один адрес) — возможно, это вообще одна и та же точка, дублированная в обеих базах агентов по ошибке.

## Обновление 18.09.2026 (Фаза 33)

Пользователь: «пересобери и сделай всё что записали добавь ещё и долги новые с датой отгрузки» — с тремя новыми файлами (Анализ Бегимай.xls, Анализ Татьяна.xls, Долги на 18.09 агентдень.xlsx).

**Часть 1 — реализован бэклог**: фильтр по агентам на странице «Задачи» (`task-filter-ownerId`, видим только супервайзеру/админу) — по образцу `filter-ownerId` на «Клиентах». Фильтрует по `assigneeId` задачи, общий для всех четырёх воронок (визиты/продажи/лист ожидания/агенту) через уже существующую `matchesTaskListFilters()`. Проверено через Playwright: 2 тестовые задачи на разных агентов → без фильтра видно 2, с фильтром на одного агента — 1.

**Часть 2 — срез по Бегимай и Татьяне на 18.09.** Оба новых файла — за период 01.09.26–18.09.26 (продолжение среза Фазы 32, не только 14–18 сентября, а весь месяц заново, как и раньше — файлы агентского учёта всегда отдают накопительный итог с начала месяца). Разобраны тем же способом (жирная строка = клиент, обычная = товар, строки «Реализация NNN» пропущены как дубли, сумма товарных строк сверена с «Итого» файла): Бегимай — 41 клиент, ₸218 709 (сошлось с «Итого» день в день); Татьяна — 76 клиентов, ₸828 874 (сошлось). В `current_month_sales.json` полностью заменены данные ТОЛЬКО этих двух агентов (остальные 4 — Батаева/Альбина/Жанара/Нагима — не тронуты, их данные из Фазы 32 остались как были).

Найдено при сверке по чек-листу совпадений: 3 клиента («Natural Beauty», «Звезда салон красоты», «Салон красоты Эксперт») встречаются В ОБОИХ новых файлах одновременно (Бегимай И Татьяна показывают продажи туда за один и тот же период) — сумма НЕ сложена вдвое, оставлена цифра из файла Бегимай (её собственная карточка), продажа Татьяны в это же место — пропущена, чтобы не задвоить выручку. Ещё 9 клиентов из файла Татьяны совпадают по названию+адресу с уже существующими карточками Бегимай (в файле Татьяны их нет), обновлены на цифру Татьяны. **Это не решено окончательно, а мой компромисс — пожалуйста, проверьте**: похоже, что часть точек фактически общая между Бегимай и Татьяной (соседние/пересекающиеся маршруты), но в системе есть карточка только у одного из двух агентов. Список этих 12 точек — в `claude/crm-mvp-status.md`.

4 действительно новых клиента добавлены в `agents_clients.json`: у Бегимай — «Салон Лилия», «Салон Малена», «Салон Твой стиль Ахунбаева»; у Татьяны — «OcOO "DSCA"» (плюс `Частное лицо` из обоих файлов сопоставлено с уже существующими карточками `Частное лицо (Бегимай)`/`Частное лицо (Татьяна)` из Фазы 32, не задвоено). «Ascorp Barbershop» у Татьяны — тоже новая карточка, но то же название уже существует пустой карточкой у Альбины (старое, не в этом срезе) — известное ограничение сопоставления по имени без учёта агента, обе карточки покажут одинаковую сумму продаж.

**Часть 3 — долги с датой отгрузки.** Файл «Долги на 18.09 агентдень.xlsx» — колонки Контрагент/Долг/Дата поставки/Агент/День недели, 121 строка долгов (без строки «ИТОГО», она исключена при разборе и использована для сверки: ₸1 681 171 — сошлось). Колонка «Дата поставки» — это и есть «дата отгрузки» из просьбы пользователя: она уже поддерживалась существующим импортом долгов (`DEBT_DATE_ALIASES` в `src/api.js` уже включает «дата поставки» первым пунктом) и уже отображается в интерфейсе — бейдж «Долг» во всплывающей подсказке и на карточке клиента («Задолженность ... — на 18.09.26»). Колонки «Агент»/«День недели» не использованы — те же данные уже приходят из `agents_clients.json` через сопоставление по имени клиента (известное ограничение — долги, как и раньше, сопоставляются только по названию клиента, без учёта агента). Данные записаны в `data/import/debts.json` в том же формате, что использует runtime-эндпоинт `/api/debts/import` (`client_name`/`debt_amount`/`is_overdue`/`payment_date`), чтобы подхватывались автоматически при каждом перезапуске сервера через `runImportBody()`, как и остальные файлы в `data/import/`.

Просрочка (`debtOverdue`) считается тем же способом, что и в существующем runtime-эндпоинте — «дата поставки уже в прошлом» (в файле нет отдельной колонки «просрочено»). Из-за этого почти все 121 строки помечены как просроченные (111 из 121), поскольку дата отгрузки по смыслу почти всегда уже прошла на момент выгрузки — это не баг, а то же самое допущение, что уже было в коде до этой сборки (см. комментарий в `src/api.js` у `/api/debts/import`), просто раньше туда не загружали файл с «датой отгрузки» вместо оплаты. Возможно, стоит пересмотреть логику «просрочки» отдельно — не стал менять без вашего решения.

**Проверка**: чистая установка (без `data/db.json`) — импорт отработал (714 контрагентов), `node --check` без ошибок на `public/app.js` и всех файлах `src/`. Через API: сумма `currentMonthRevenue` по всем клиентам ₸2 597 461, сумма `debtAmount` ₸1 098 207 (часть из 121 строки долга не сопоставилась ни с одной карточкой — 30 названий не найдены в базе, то же самое ограничение по сопоставлению только по имени, что и раньше). Точечно проверены Natural Beauty (₸5775, без задвоения), Ascorp Barbershop (новая карточка у Татьяны, ₸2045), Частное лицо (Бегимай)/(Татьяна) (не задвоены).

## Бэклог на следующую сборку (зафиксировано, реализация не начата)

(пусто — единственный пункт из прошлой сборки, фильтр по агентам в «Задачах», реализован в Фазе 33 выше)

## Запуск локально (для проверки)

Нужен установленный Node.js версии 18 или новее.

```bash
npm start        # запускает сервер на http://localhost:3000
```

При первом запуске (когда база пуста) приложение **само создаёт** команду и подтягивает данные контрагентов, ассортимента и долгов из `data/import/*.json` — никаких дополнительных команд вводить не нужно.

Логины по умолчанию (**обязательно смените пароли** после первого входа во вкладке «Команда»):

| Роль | Email | Пароль |
|---|---|---|
| Администратор | `admin@cosmedica.local` | `admin123` |
| Супервайзер (Александр) | `alexandr@cosmedica.local` | `super123` |
| Агент (Нагима) | `nagima@cosmedica.local` | `agent123` |
| Агент (Татьяна) | `tatiana@cosmedica.local` | `agent123` |
| Агент (Альбина) | `albina@cosmedica.local` | `agent123` |
| Агент (Батаева) | `bataeva@cosmedica.local` | `agent123` |
| Агент (Жанара) | `zhanara@cosmedica.local` | `agent123` |
| Агент (Бегимай) | `anastasia@cosmedica.local` | `agent123` |

Если нужно начать с полностью чистой базы — удалите файл `data/db.json` и перезапустите сервер (данные контрагентов/ассортимента/долгов из `data/import/` подтянутся заново, пользователи создадутся заново с паролями по умолчанию).

## Как разместить приложение в интернете, чтобы работала команда

Подробная пошаговая инструкция с прямыми ссылками — в файле **ИНСТРУКЦИЯ_GITHUB_RENDER.md**. Коротко: код заливается на GitHub, а сервис Render.com берёт его оттуда и запускает по постоянной ссылке.

**Важно про данные**: на бесплатном тарифе Render диск не сохраняется постоянно — при обновлении или перезапуске содержимое `data/db.json` и загруженные вложения (`data/uploads/`) может обнулиться. Для реальной работы (когда терять клиентов, задачи и вложения нельзя) нужен платный план Render с постоянным диском (от $7/мес) либо обычный VPS — подключение диска подробно описано в инструкции.

## Обновление данных (продажи, долги, новые контрагенты)

Приложение не подключено напрямую к Google Диску (у него нет доступа/паролей от вашего аккаунта) — обновление происходит вручную, но просто:

1. Загружайте свежие выгрузки в две папки на Google Диске, созданные для этого:
   - **папка для актуальных продаж** — файлы по продажам за новые периоды;
   - **папка для актуальных долгов** — свежая выгрузка по задолженности.
2. Напишите об этом в следующем разговоре с Claude (в этом же проекте на claude.ai) — попросите пересчитать регулярный ассортимент и долги по новым файлам.
3. Claude обновит файлы `data/import/regular_assortment.json`, `data/import/test_assortment.json` и `data/import/debts.json`, вы получите обновлённый архив проекта — залейте его поверх старого на GitHub (см. инструкцию, часть «Что дальше»), Render пересоберёт сайт автоматически.
4. При перезапуске сервер сам подтянет новые данные: новых контрагентов добавит, а вручную исправленные администратором карточки (адрес, телефон, договор и т.п.) не тронет — обновятся только расчётные поля (ассортимент, долг).

## Резервные копии

Все данные — это файл `data/db.json` и папка `data/uploads/`. Периодически скачивайте их с сервера (или, если используете платный диск на Render, делайте снапшот через панель Render) в надёжное место. Потеря `data/db.json` = потеря всех клиентов, задач и истории визитов.

## Структура проекта

```
src/
  server.js    — HTTP-сервер, раздача фронтенда, отдача вложений
  router.js    — минимальный роутер
  db.js        — файловая база данных (JSON)
  auth.js      — пароли (хеширование) и сессии
  api.js       — вся бизнес-логика: клиенты, задачи, вложения, пользователи
  multipart.js — разбор загрузки файлов (фото/аудио)
  import.js    — импорт/обновление контрагентов, ассортимента, долгов при запуске
  miniZip.js   — сборка ZIP-архива для резервной копии (без внешних библиотек)
public/
  index.html, styles.css, app.js  — интерфейс (без сборки, обычный JS)
  sw.js        — минимальный service worker (нужен только для уведомлений в фоне)
data/
  db.json      — все данные приложения (создаётся автоматически)
  uploads/     — загруженные фото/аудио по задачам
  import/      — исходные данные для импорта (контрагенты, регулярный/тестовый ассортимент, долги, остатки, акции, артикулы)
```

## Известные ограничения MVP

- **«Риск отвала ассортимента» — приближение, а не точный расчёт по дням.** Исходные данные о продажах помесячные (не по датам), поэтому товар помечается «риск», если его не покупали в последнем доступном месяце — это приближение к правилу «не заказывал 14+ дней», а не точный расчёт.
- **«Тестовый ассортимент»** считается по тем же помесячным данным за 7 месяцев — это товары с 1–3 месяцами покупок из 7 (регулярные — от 4 месяцев), а не «купил один раз за всё время работы».
- **Способ оплаты** нужно проставить вручную по каждому клиенту — в исходных таблицах агентов не нашлось отдельного столбца с этими данными.
- **Статус «работает по договору»** заполнен только там, где он был в исходных таблицах (сейчас это данные по точкам Альбины); для остальных клиентов стоит «неизвестно» — уточните и укажите вручную по мере необходимости.
- Обновление продаж/долгов — не автоматическое: см. раздел «Обновление данных» выше.
- Нет смены пароля из интерфейса (пока — через переустановку пользователя администратором).
- **Уведомления о задачах работают, только пока открыта вкладка приложения** (можно свернуть её или переключиться на другое окно — это нормально), но не при полностью закрытом браузере. Настоящие серверные push-уведомления потребовали бы отдельного облачного сервиса и ключей — сознательно не подключали, чтобы не нарушать принцип «ноль внешних зависимостей». Если это критично, можно обсудить подключение позже.
- **«Печать маршрута»** — это системная печать браузера (Ctrl+P / диалог печати), а не готовый файл PDF, который генерирует сервер; чтобы получить PDF-файл, в диалоге печати нужно выбрать «Сохранить как PDF».
- Нет email/SMS-уведомлений о задачах.
- Файловая БД устраивает команду до ~10-15 активных пользователей и умеренного объёма данных; для более серьёзной нагрузки понадобится настоящая СУБД.
- **Бренд товара определяется по ключевым словам в названии** (Kapous/EPICA/Чистовье/Палитра), а не по отдельному столбцу в исходных файлах — в редких случаях товар может попасть в «Прочее», даже если по факту относится к одному из брендов; список ключевых слов можно расширить.
- **Записи встреч (аудио) хранятся на диске сервера** — на бесплатном тарифе Render, как и остальные вложения, они не гарантированно переживают перезапуск (см. раздел «Важно про данные» выше про постоянный диск).
- **Встречи супервайзера — это отдельная лёгкая «бронь дня»**, не полноценная задача: без этапов, отчётов и напоминаний — только видимость в календаре, кто когда занят.
- **Остаток на складе на позиции ассортимента — это срез на дату выгрузки** (25.08.2026), не живая синхронизация со складом; обновляется вручную вместе с остальными данными (см. «Обновление данных» выше). Примерно 11% позиций не находят соответствия в остатках из-за обрезанных до 70 символов названий товаров в исторических файлах продаж — для них бейдж просто не показывается.
- **«Акции» — тоже срез на дату**, не привязаны к конкретной задаче/визиту, показывают только сам факт и количество.
- **Совпадения имени контрагента между разными агентами** не создают вторую карточку — данные текущего месяца сопоставляются по имени без учёта агента, поэтому обе (все) одноимённые карточки показывают одну и ту же сумму. «Студия Vivienne Lashes» (Альбина/Бегимай, разные адреса) — **подтверждено пользователем 14.09.2026: это два разных салона**, но карточки пока намеренно НЕ разнесены (правка отложена на будущее по прямой просьбе пользователя). «Азбука тела Низ» (Татьяна/Альбина, один и тот же адрес — возможно, дубль одной точки) — решение ещё не принято. Подробности в разделе «Обновление 14.09.2026 (Фаза 32)» выше. **Ещё 12 точек, найденных 18.09.2026 (Фаза 33)** — у Бегимай и Татьяны, в срезах за один и тот же период, встречаются клиенты с одинаковым названием+адресом (например, «Natural Beauty», «Звезда салон красоты», «Остео Клиник Токонбаева 23/4») — похоже на пересекающиеся маршруты двух агентов, а не на случайное совпадение имён. Пока продажи не задвоены (оставлена сумма только с одной стороны), но решение, кому из агентов на самом деле принадлежит каждая из этих точек, не принято — список см. в «Обновление 18.09.2026 (Фаза 33)» и `claude/crm-mvp-status.md`.
- **Артикул товара проставлен не у всех позиций** (~92% ассортимента) — прайс-листа на 5-й бренд (декоративная косметика, «AV/ES/PRO/MS») не присылали, плюс часть товаров с нетиповыми/обрезанными названиями не находит пару в прайсах; если пришлёте недостающий прайс — досчитаем.
- **«Последние продажи» на карточке клиента — это последние 3 месяца с продажами, а не настоящие накладные** — номеров и дат отдельных документов поставки в исходных данных нет, только помесячные суммы по товару.

## Небольшая заметка по качеству исходных данных

При импорте обнаружено: в таблице Нагимы дважды встречается «Салон Sofiline» (одинаковый адрес, разные контакт/телефон) — похоже на дубль в исходной таблице или два контакта одной точки. Система создала одну карточку клиента; если это два разных контакта одной точки — второй телефон/имя можно дописать вручную в заметки карточки.

При добавлении Батаевой/Жанары/Анастасии обнаружено: в файлах их таблиц после основного списка контрагентов идёт повторяющийся блок с суммами долга (одинаковый почти во всех трёх файлах, включая уже импортированный Реализация/Долги) — это, судя по всему, общий сводный отчёт по компании, случайно попавший в каждую личную таблицу агента, а не список именно его клиентов. Этот блок не импортировался.
